plugins {
    id 'java'
    id 'org.springframework.boot'
    id 'io.spring.dependency-management'
}

dependencies {
    implementation(libs.payment.card.network)
}

tasks.named('test') {
    useJUnitPlatform()
}
