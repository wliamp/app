plugins {
    id 'java'
    id 'org.springframework.boot'
    id 'io.spring.dependency-management'
}

dependencies {
    implementation(libs.banking.issuer)
}

tasks.named('test') {
    useJUnitPlatform()
}
