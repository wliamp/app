name: Overview

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  overview:
    if: >
      contains(vars.INCLUDE_OVERVIEW_ACTORS, github.actor)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate Repo Overview
        run: bash .github/scripts/overview.sh

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "auto update repo overview" || echo "No changes to commit"

      - name: Create or update PR
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.PAT }}
          branch: auto/overview
          title: "Overview"
          body: |
            This pull request updates the **README.md** file with the latest automatically generated overview.

            - 🧩 Repo structure
            - ⚙️ Workflow summary
            - 🔒 Ruleset overview

            Please review and **squash-merge** to keep linear history clean.
          commit-message: "Auto update repository overview"
          delete-branch: true
