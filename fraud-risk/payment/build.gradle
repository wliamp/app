plugins {
    id 'java'
    id 'org.springframework.boot'
    id 'io.spring.dependency-management'
}

dependencies {
    implementation(libs.fraud.risk.payment)
}

tasks.named('test') {
    useJUnitPlatform()
}
